{
  "name": "ct-server",
  "version": "1.0.0",
  "description": "Server-side code for the Computer Tutor application.",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && webpack --mode development && webpack --mode production",
    "buildDev": "rm -rf dist && webpack --mode development",
    "buildProd": "rm -rf dist && webpack --mode production",
    "dev": "NODE_ENV=development nodemon ./src/server.js",
    "debug": "NODE_ENV=development nodemon inspect ./src/server.js",
    "start": "NODE_ENV=production ./src/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/ethorne/computer-tutor.git"
  },
  "author": "Erik Thorne",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ethorne/computer-tutor/issues"
  },
  "homepage": "https://github.com/ethorne/computer-tutor",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "path": "^0.12.7",
    "validate": "^4.5.1"
  },
  "devDependencies": {
    "@babel/core": "^7.4.0",
    "@babel/preset-env": "^7.4.3",
    "babel-loader": "^8.0.5",
    "nodemon": "^1.18.10",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0",
    "webpack-dev-server": "^3.2.1",
    "webpack-node-externals": "^1.7.2"
  }
}
